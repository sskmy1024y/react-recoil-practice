import {todoListState, } from "../store/Todo/atom";
import {useRecoilState} from "recoil";

export const ExampleAtomFamily = () => {
  // const [todoList, setTodoList] = useRecoilState(todoWithKeyState("aa"))
  //
  // const handleAddTodo = () => {
  //   setTodoList(prev => ([...prev, "TODO"]))
  // }
  //
  // return (
  //   <div>
  //     <button onClick={handleAddTodo}>{"Add tasks"}</button>
  //     <div>
  //     {todoList.map((todo, index) => {
  //         return <div key={index}>{`${todo} ${Date.now()}`}</div>
  //       })}
  //     </div>
  //   </div>
  // );

  return null
}

